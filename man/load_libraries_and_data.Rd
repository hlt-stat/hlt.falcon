% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/falcon02.R, R/falcon22.R
\name{load_libraries_and_data}
\alias{load_libraries_and_data}
\title{Load Libraries and Data}
\usage{
load_libraries_and_data()

load_libraries_and_data()
}
\value{
A list containing two dataframes: 'anl', which is the result of joining the 'adsl' and filtered 'advs' data on the 'USUBJID' column, and the original 'adsl' dataframe.

A list containing the dataframe for analysis and the original adsl dataframe.
}
\description{
This function loads the necessary libraries and data for the analysis. It imports the 'haven' package to read the 'adsl' and 'advs' data from the specified paths, filters the 'advs' data for the baseline visit, selects the 'USUBJID' and 'AVAL' columns, and then joins the 'adsl' and filtered 'advs' data on the 'USUBJID' column. The paths to the 'adsl' and 'advs' data are currently hard-coded and need to be updated for different datasets.

This function loads the necessary libraries and data for the analysis.
}
\examples{
\dontrun{
data <- load_libraries_and_data()
# 'data' is a list containing 'anl' and 'adsl' dataframes
}
\dontrun{
data <- load_libraries_and_data()
}
}
